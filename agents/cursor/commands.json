{
  "commands": {
    "arags-search": {
      "command": "arags search \"$ARGUMENTS\" --project . --format jsonl",
      "description": "Search code with hybrid RAG search"
    },
    "arags-context": {
      "command": "arags search \"$ARGUMENTS\" --project . --format text",
      "description": "Retrieve project context as prompt text"
    },
    "arags-query": {
      "command": "arags query \"$ARGUMENTS\" --qa",
      "description": "Analytical QA digest via the user's local LLM"
    },
    "arags-index": {
      "command": "arags init && arags index .",
      "description": "Index project for RAG search"
    }
  }
}
