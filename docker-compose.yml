services:
  arlm:
    build: .
    volumes:
      - arlm-data:/home/arlm/.arlm
    ports:
      - "8420:8420"
    environment:
      - ARLM_DATA_DIR=/home/arlm/.arlm
    command: ["serve", "--host", "0.0.0.0", "--port", "8420"]

volumes:
  arlm-data:
