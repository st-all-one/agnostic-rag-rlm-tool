syntax = "proto3";
package arlm.v1;

// ── Server Management ──

message ServerStatus {
  string version = 1;
  int32 uptime_seconds = 2;
  int32 active_runs = 3;
  int32 total_projects = 4;
  int64 total_chunks = 5;
  reserved 6, 8; // removed: total_summaries, summarize (plan 019/020: no server-side summarizer)
  reserved "total_summaries", "summarize";
  WriteQueueStats write_queue = 7;
}

message WriteQueueStats {
  int32 pending_writes = 1;
  int32 batched_last_flush = 2;
  double avg_latency_ms = 3;
}
